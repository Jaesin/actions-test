name: CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v1
    - name: list files
      run: |
        pwd
        ls -hal --color
        which php
        which composer
        php --version
        composer --version


